# 揭棋英雄与畸变系统代码规格 v0.1

> 状态：规则确认稿；核心权威规则、房间数据边界、试玩入口和自动测试已实现，真实云端适配待接入  
> 日期：2026-08-31  
> 基线：`jieqi-engine` v0.4.0 与《揭棋规则规格 v1》  
> 目标：把三名英雄、六项畸变和新增终局转换为可直接编码与测试的服务器权威规则

## 1. 范围与兼容原则

本文只定义英雄与畸变系统，不修改已经完成的 v0.4.0 本地试玩基线。实现时应在新版本中增量扩展，确保关闭英雄与畸变后，现有 71 项测试仍全部通过。

规则优先级从高到低为：

1. 双方将帅死亡与直接死亡终局；
2. 英雄、畸变产生的路径击杀、陷阱和防御；
3. 暗子揭示、控制权变化与背刺；
4. 普通将军、裁决、无处可逃；
5. 回合切换与持续时间递减。

每局最多随机生效一项畸变。不同畸变之间不叠加，但畸变可以和双方英雄能力叠加。

## 2. 对局建立流程

房间分别保存两个开关：

```ts
interface OptionalModeConfig {
  heroesEnabled: boolean;
  mutationsEnabled: boolean;
}
```

四种组合都必须合法：

| 英雄 | 畸变 | 流程 |
|---|---|---|
| 关闭 | 关闭 | 猜拳 → 建局 → 红方行动 |
| 开启 | 关闭 | 猜拳 → 选英雄 → 建局 → 必要的陷阱布置 → 红方行动 |
| 关闭 | 开启 | 猜拳 → 建局并公开畸变 → 红方行动 |
| 开启 | 开启 | 猜拳 → 选英雄 → 建局并公开畸变 → 必要的陷阱布置 → 红方行动 |

### 2.1 英雄选择

- 猜拳完成并确定红黑后才选择英雄。
- 双方同时秘密选择；一方锁定后，对方只能看到“已锁定”。
- 双方都锁定后同时公开。
- 允许选择相同英雄。
- 锁定后不能更换。
- 英雄关闭时，不创建英雄选择阶段或英雄状态。

### 2.2 畸变抽取

- 正式棋盘创建时，由服务器从六项畸变中等概率抽取一项。
- 使用服务器安全随机数；测试可注入固定随机源。
- 抽取结果立即向双方公开并记录在棋局状态中。
- 畸变关闭时使用 `null`，不得抽取后隐藏。

### 2.3 猎人布置阶段

- 只要至少一方选择猎人，正式走棋前进入 `trap_setup`。
- 每名猎人独立提交两个陷阱层；可以分开放置，也可以重叠在同一点。
- 双猎人同时秘密布置，双方提交完成后进入红方第一回合。
- 非猎人只看到对方是否完成布置，不能获得坐标。

建议房间阶段：

```ts
type RemoteRoomPhase =
  | "waiting"
  | "rps"
  | "hero_selection"
  | "trap_setup"
  | "playing"
  | "finished";
```

畸变展示动画属于客户端表现，不单独阻塞服务器阶段。

## 3. 公共类型与状态模型

### 3.1 标识类型

```ts
type HeroId = "hunter" | "rogue" | "warrior";

type MutationId =
  | "iron_steed"       // 铁马
  | "iron_wall"        // 铁壁
  | "shadow_dance"     // 暗影之舞
  | "war_chariot"      // 战车
  | "expedition"       // 出征
  | "cavalry";         // 骑兵

type SkillSource = "hero" | "mutation";
```

### 3.2 棋子公开效果

所有特殊效果都公开显示；暗子真实身份仍然保密。

```ts
interface BarrierEffect {
  kind: "barrier";
  enemyHalfEntered: boolean;
  movesAfterEnemyHalfEntry: 0 | 1;
}

interface StealthEffect {
  kind: "stealth";
  owner: Side;
  remainingOwnerTurns: 1 | 2;
  strongStrikeAvailable: boolean;
  source: SkillSource;
}

interface PieceEffects {
  barrier?: BarrierEffect;
  stealth?: StealthEffect;
  cavalry?: true;
}

type PieceEffectsById = Record<string, PieceEffects>;
```

`cavalry`、`barrier` 和 `stealth` 均随棋子 ID 保存。棋子控制权改变不会清除这些效果。棋子死亡时删除对应运行时效果。

### 3.3 英雄公开状态

```ts
interface HunterPublicState {
  hero: "hunter";
  setupComplete: boolean;
}

interface RoguePublicState {
  hero: "rogue";
  heroChargeAvailable: boolean;
  mutationChargeAvailable: boolean;
  activePieceId?: string;
}

interface WarriorPublicState {
  hero: "warrior";
  barrierPieceIds: string[]; // 最多两个，名额使用后不回补
  ironArmorAvailable: boolean;
}

type HeroPublicState =
  | HunterPublicState
  | RoguePublicState
  | WarriorPublicState;
```

非潜行者在“暗影之舞”中也建立刺杀次数状态。建议不要把畸变次数塞进 `RoguePublicState`，正式实现可抽成双方通用的 `AssassinationState`：

```ts
interface AssassinationState {
  heroChargeAvailable: boolean;
  mutationChargeAvailable: boolean;
  activePieceId?: string;
}
```

### 3.4 私有陷阱状态

```ts
interface TrapLayer {
  id: string;
  owner: Side;
  position: Position;
  opponentTurnsRemaining: number; // 初始为 10
}

interface HeroSecretState {
  traps: TrapLayer[];
}
```

陷阱坐标不能进入双方共享的公共房间文档。联机实现应新增按玩家隔离的私有视图，或通过鉴权云函数动态返回自己的陷阱；刷新页面后，猎人必须能够恢复自己的陷阱显示。

### 3.5 终局结果

现有 `winner?: Side` 不能表达平局，建议升级为不可组合出无效状态的联合类型：

```ts
type WinReason =
  | "ambush"             // 背刺，保留旧 wire value
  | "checkmate"          // 裁决
  | "stalemate"          // 无处可逃
  | "resign"             // 臣服
  | "disconnect"         // 虚空放逐
  | "trap_ambush"        // 伏击
  | "crush_them"         // 碾碎他们！
  | "rampage";           // 乱杀

type DrawReason = "mutual_destruction"; // 两败俱伤

type GameOutcome =
  | { kind: "win"; winner: Side; loser: Side; reason: WinReason }
  | { kind: "draw"; reason: DrawReason };
```

迁移期间可继续派生旧 `winner` 字段，但 `outcome` 应成为唯一权威结果。

## 4. 英雄一：猎人——陷阱

### 4.1 布置

- 猎人在走棋开始前布置两个陷阱层。
- 只能布置在猎人自己的半场：红方 `y >= 5`，黑方 `y <= 4`。
- 可以放在已有棋子的脚下。
- 两层可以放在同一坐标。
- 猎人始终能看到自己的剩余陷阱；敌方在触发前看不到坐标和叠层数。

### 4.2 触发条件

一次行动成功落位后，同时满足以下条件才触发一层陷阱：

1. 落点存在敌方猎人的陷阱；
2. 该棋子在行动开始前由陷阱主人的敌方控制；
3. 移动、吃子和揭示完成后，该棋子仍由陷阱主人的敌方控制。

这两个控制权条件用于防止猎人用陷阱低成本鉴定己方暗子的真实身份。

被目标防御弹回原位不视为到达目标点，不触发目标点陷阱。路径击杀也不触发死者所在点或经过点的陷阱。

### 4.3 触发效果

- 一次进入只触发并消耗一层；同点剩余层继续存在。
- 陷阱直接杀死落点棋子，不属于“被棋子吃掉”。
- 普通防御、隐身和铁甲防御都不能阻止陷阱。
- 可以杀死敌方将帅；触发终局“伏击”。
- 双方都收到触发事件和播报：`猎物已踏入陷阱！伏击得手，红方／黑方获得胜利！`

### 4.4 持续时间

- 每层陷阱持续十个敌方正式走棋回合。
- 敌方第十回合的全部行动效果结算时，陷阱仍然有效。
- 第十回合结算完成后，未触发的剩余层失效。
- 猜拳、选英雄、布置、自动终结动画和战士额外应将均不计入十回合。

## 5. 英雄二：潜行者——刺杀

### 5.1 次数与目标

- 潜行者英雄每局提供一次刺杀。
- “暗影之舞”畸变再向双方各提供一次刺杀。
- 潜行者遇到暗影之舞时最多使用两次，两个来源分别记录。
- 同一方同时只能维持一段刺杀潜行；上一段结束前不能开启下一次。
- 发动目标必须是己方控制的明棋，且不能是将帅。

### 5.2 发动行动

发动刺杀和首次走子属于同一个服务器原子命令：

```ts
interface AssassinationCommand {
  kind: "assassination";
  from: Position;
  to: Position;
  useStrongStrike: boolean;
  expectedRevision: number;
  actionId: string;
}
```

- 首次行动可以移动到空位，也可以吃子。
- 必须遵守该棋子当前全部移动规则、畸变限制、铁壁和自陷将军规则。
- 若首次行动使用强击，目标必须存在且不能是将帅；结算后不进入隐身，整次刺杀结束。
- 若未使用强击，行动结束后进入隐身并保留一次强击。

### 5.3 隐身

- 隐身状态和剩余回合向双方公开显示。
- 隐身持续接下来的两个己方回合，不包括发动回合。
- 对方回合不消耗持续时间。
- 隐身棋子不产生攻击范围和将军。
- 不能被普通吃子或其他普通指向效果选择；刺杀强击可以选择并先移除其隐身。
- 不阻挡其他棋子的移动路径，不充当炮架、马腿或象眼。
- 所在格仍然被占用，其他棋子不能停在同一格。
- 陷阱、铁马碾碎和战车路径碾碎仍可将其杀死。

在两个后续己方回合中的任意一个回合，玩家可以主动移动或吃子这枚棋：

- 行动开始后解除隐身；
- 可以在一次合法吃子上使用保留的强击；
- 不使用强击也会在行动完成后失去该次强击；
- 若两个回合都没有主动使用该棋，第二个己方回合结算后自动解除隐身并清除强击。

### 5.4 强击

- 强击沿用来源棋子的合法移动与吃子几何。
- 不能以将帅为目标。
- 可以选择正常不可被指定的隐身棋子。
- 目标先失去防御、隐身及其他可移除效果，再被直接杀死。
- 强击属于特殊吃子，触发吃子事件和死亡事件。
- 强击完成后攻击者落在目标点，随后照常结算陷阱。
- 使用强击立即结束或阻止本次隐身。

铁马与战车的“等同强击”只复用“清除效果并击杀”的结算模板，不消耗刺杀强击次数。

### 5.5 攻击战士防御

- 普通刺杀攻击撞上防御：目标消耗防御并存活，攻击者回到原位。
- 若这是刺杀发动时的首次行动，攻击者在原位进入隐身并保留强击。
- 若这是隐身期间的后续普通攻击，攻击者解除隐身，未用强击同时失效。
- 使用强击则直接移除目标防御并杀死目标，不会被弹回。

## 6. 英雄三：战士——防护壁垒

### 6.1 普通防御的获得

- 每名战士最多让两枚不同的己方非将帅明棋获得普通防御。
- 触发条件：棋子在一步成功行动中从战士自己的九宫内部移动到九宫外部。
- 按实际成功触发的先后占用两个名额。
- 每个棋子一局最多获得一次；防御消耗、到期或棋子死亡都不返还名额。
- 将帅离开九宫不获得普通防御。
- 防御及剩余衰减状态向双方公开，并直接显示在棋面上。

基础规则规定暗子第一次移动后立即翻开；暗仕第一次又只能在九宫内移动。因此“暗子保持暗面离开九宫后才翻开并获得防御”在当前规则下不可到达，不进入实现。未来若新增“移动后仍不翻开”的效果，再重新定义该分支。

### 6.2 普通防御的效果

普通吃子命中带防御棋子时：

1. 消耗目标的一层防御；
2. 目标留在原位；
3. 攻击者返回行动前原位；
4. 本回合仍然结束；
5. 暗攻击者保持暗面，不读取或公开真实身份；
6. 不触发目标点、返回点陷阱或进入/离开区域效果。

路径碾碎、刺杀强击和陷阱不属于可被普通防御抵挡的攻击。

### 6.3 防御衰减

- 防御棋子第一次进入敌方半场时开始衰减计数，进入动作本身不计一步。
- 此后该棋子每完成一次成功移动，计数加一。
- 完成第二次成功移动后移除防御。
- 中途返回己方半场不重置、不暂停计数。
- 失败、被弹回或被服务器拒绝的行动不计数。

### 6.4 将帅铁甲防御

- 战士将帅开局拥有公开的 `ironArmorAvailable = true`。
- 铁甲防御只免疫一次“背刺”，不抵挡陷阱、强击、路径碾碎、裁决或其他终局。
- 背刺被免疫后，倒戈棋子留在棋盘上，仍然对战士将帅形成普通将军。
- 战士立即获得一次只用于解除该将军的额外应将行动。
- 如果不存在合法应将，铁甲虽已消耗，仍由对方以“裁决”获胜。
- 应将完成后轮到原本应行动的对方；该额外应将不消耗陷阱的敌方回合持续时间。

建议用显式阶段表达，不要偷偷修改普通回合计数：

```ts
interface ForcedDefenseState {
  responder: Side;
  resumeTurn: Side;
  cause: "iron_armor_blocked_backstab";
}
```

## 7. 畸变一：铁马

### 7.1 适用棋子

- 暗马位棋子第一次按位置兵种移动时适用。
- 已揭明且真实兵种为马的棋子适用。
- “骑兵”是另一项互斥畸变，因此不会与铁马叠加。

### 7.2 碾碎马腿

- 马选择一个合法日字目标时，计算唯一马腿格。
- 马腿为空：按普通马步行动。
- 马腿存在任意棋子：先对该棋执行路径碾碎，再继续完成马步。
- 路径棋无论敌友、明暗、隐身或是否拥有防御，都会先清除效果再死亡。
- 暗路径棋公开真实身份并进入被吃记录。
- 铁马可以移动到空位并仅靠途中碾碎获得击杀。
- 玩家不能选择保留马腿棋再跳过。

目标点按普通移动、普通吃子或刺杀强击结算。若最终目标以普通防御抵挡，马返回原位，但马腿棋保持死亡。

### 7.3 铁马将军

铁马对将帅存在两类威胁：

1. 将帅位于马步最终落点；
2. 将帅位于马腿格，且该方向至少有一个可完成的合法马步落点。

第二类属于路径碾碎将军。敌方将帅作为马腿被杀时触发“碾碎他们！”。

## 8. 畸变二：铁壁

- 敌方控制的棋子不能从九宫外移动、吃子或使用技能进入该方九宫。
- 判定使用行动开始前的公开控制权，不读取暗子真实身份。
- 已在九宫内的敌方棋子可以在宫内移动、吃子或离开；离开后不能重新进入。
- 暗子在己方九宫内行动后可能翻成敌色并成为内部渗透者，该结果合法。
- 隐身和强击不能绕过铁壁。
- 攻击线与将军判定可以跨越铁壁；普通棋子不能从外部落入九宫吃子。
- 将死的自动终结动画可以跨越边界，因为此时只表现已确定的终局。

铁壁只限制“从外到内”的落点，不限制路径穿越九宫边界、不限制离开，也不删除九宫内已有棋子。

## 9. 畸变三：暗影之舞

- 双方各获得一次完整“刺杀”技能。
- 次数独立、公开且一直保留到使用或对局结束。
- 非潜行者也可以使用。
- 潜行者英雄同时拥有英雄次数与畸变次数，共两次。
- 两个来源不能合并成一次强化效果；每次分别产生一段隐身和一次强击。
- 同一方上一段隐身结束前不能发动下一次。

## 10. 畸变四：战车

### 10.1 适用棋子与发动条件

- 暗车位棋子第一次按位置兵种行动时适用。
- 已揭明且真实兵种为车的棋子适用。
- 必须选择同一直线上的一个实际目标，不能以空位为目标。
- 车与目标之间必须恰好存在一枚非隐身棋子。
- 任意数量的隐身棋子不参与“恰好一枚”的计数。
- 目标仍须符合普通目标规则：暗目标始终可吃、敌方明目标可吃、己方明目标不可吃；将帅不作为普通目标，只在将军与自动终结中作为概念目标。

### 10.2 结算

1. 收集起点与目标之间的全部路径棋。
2. 对唯一非隐身路径棋和全部隐身路径棋执行路径碾碎。
3. 路径棋先清除防御、隐身等效果，再直接死亡。
4. 对最终目标执行普通吃子；刺杀强击可改为强击目标。
5. 目标普通防御成功时，车返回原位，但所有路径死亡保持生效。
6. 成功到达目标后，暗车位来源才翻开，随后结算控制权和陷阱。

路径棋可以是己方明棋。路径击杀不消耗潜行者强击次数。

### 10.3 战车将军

- 车与敌方将帅之间没有非隐身棋子：构成普通车将军；隐身棋子不阻挡。
- 恰好一枚非隐身棋子：构成战车将军，无论该棋敌友或是否拥有防御。
- 两枚及以上非隐身棋子：不构成将军。
- 唯一非隐身路径棋是进攻方将帅时，仍构成将军；若最终执行，会形成同归于尽式终局。
- 敌方必须正常应将，不能因为攻击后可能平局而忽略。
- 若无合法应对，服务器按实际攻击结果播放终结，不能一律写成“裁决”。

### 10.4 暗来源的责任归属

暗车位的路径击杀发生在来源翻开以前，因此全部击杀和终局责任归属于行动开始时的控制方。来源落地后即使倒戈，也不追溯改变路径击杀结果。

## 11. 畸变五：出征

- 双方将帅解除九宫限制，可在整个棋盘沿横线或竖线移动任意距离。
- 路径必须没有阻挡；隐身棋子不阻挡路径，但其所在格仍不能作为落点。
- 将帅可以离开九宫、跨河并进入敌方半场。
- 只检查最终落点是否受攻击；途中经过受攻击空位不影响合法性。
- 将帅获得车的直线攻击范围，可以远距离将军。
- 将帅不能把敌方将帅当作普通棋子直接吃掉，仍通过将军、将死和裁决结算。
- 将帅仍不能停在敌方攻击范围内或与敌方将帅形成无法解释的互相攻击状态。
- 将帅踩中猎人陷阱时可被伏击杀死。
- 战士将帅离开九宫不获得普通防御，只保留铁甲防御。

## 12. 畸变六：骑兵

### 12.1 升级目标

棋盘创建时，把双方五个兵位中从各自视角由左到右的第 1、第 3、第 5 个位置上的暗子标记为骑兵。由于位置对称，坐标为：

- 黑方：`(0,3)`、`(4,3)`、`(8,3)`；
- 红方：`(0,6)`、`(4,6)`、`(8,6)`。

升级附着于具体棋子 ID 并公开显示。揭开后无论真实颜色为何，升级都随该棋转交给新的控制方。

### 12.2 混合走法

骑兵每次行动可以选择：

1. 当前棋种原本的走法；或
2. 马的日字几何，并同时遵守马腿及当前棋种的方向、区域和安全限制。

“共同限制”不要求一步同时满足两种几何形状，也不把炮架等原棋种专属吃子机制套到马步上。

- 暗骑兵当前按兵位身份处理，只能使用四个前向马步，不能向后方或后侧落点移动。
- 揭开为兵/卒后，前进方向按真实颜色计算，过河后仍不能用马步后退。
- 揭开为车、马、炮、相、象、仕或士后，不存在兵卒方向限制，可使用完整八方向马步。
- 所有马步都受蹩马腿限制；“铁马”与“骑兵”互斥，不会获得碾碎能力。

## 13. 路径碾碎统一模板

铁马与战车共用同一服务器内部效果，但各自拥有不同的发动几何：

```ts
interface CrushContext {
  sourcePieceId: string;
  actingSide: Side;
  cause: "iron_steed" | "war_chariot";
  pathPieceIds: string[];
}
```

对每枚路径棋按棋盘行进顺序执行：

1. 清除普通防御、隐身和其他可移除效果；
2. 公开暗子的真实身份；
3. 从棋盘移除并写入死亡/被吃记录；
4. 记录公开动画事件；
5. 将帅死亡只做标记，不提前中断本次原子行动。

路径碾碎可以杀死将帅，且不受刺杀“不能以将帅为目标”的限制，因为将帅不是强击的选择目标。

## 14. 权威行动结算管线

现有 `applyAuthoritativeMove` 应拆为“计划—验证—结算”三层，避免通过反复模拟误读秘密身份。

### 14.1 计划

```ts
interface ActionPlan {
  actionKind: "move" | "assassination" | "forced_defense";
  actingSide: Side;
  sourceId: string;
  from: Position;
  to: Position;
  movementMode: "native" | "cavalry_horse" | "iron_steed" | "war_chariot";
  pathPieceIds: string[];
  targetId?: string;
  useStrongStrike: boolean;
}
```

计划层只读取公开数据，不得根据暗子真实身份决定按钮、提示或合法落点。

### 14.2 验证

依次验证：

1. 房间阶段、行动方、版本号和幂等 ID；
2. 来源控制权和技能次数；
3. 移动几何、路径、马腿、目标资格和铁壁；
4. 隐身目标与路径的特殊占位；
5. 使用公开状态模拟行动后，检查是否无故让己方存活将帅受攻击；
6. 铁马或战车明确碾死己方将帅的行动属于允许产生终局的特殊行动，不以普通 `SELF_CHECK` 拒绝。

### 14.3 结算

一次玩家命令严格按下列顺序原子执行：

1. 消耗技能次数并创建必要的刺杀状态；
2. 执行铁马或战车路径碾碎；
3. 结算最终目标：强击、普通吃子或普通防御；
4. 攻击者落位或因防御返回原位；
5. 成功落位的暗来源揭开并更新控制权；
6. 应用或解除隐身、普通防御、骑兵等运行时效果；
7. 成功落位后检查并触发一层陷阱；
8. 检查双方将帅最终存活状态；
9. 无将帅直接死亡时判断背刺及铁甲防御；
10. 判断普通将军、裁决和无处可逃；
11. 递减刺杀持续时间和陷阱持续时间；
12. 切换或恢复回合并把版本号增加一次。

被普通防御弹回时，步骤 5 的来源揭示、目标点陷阱和区域进入/离开效果全部跳过；步骤 2 已经发生的路径死亡不回滚。

## 15. 将军与终局判定

### 15.1 统一攻击定义

`isSquareAttacked` 不能再只检查传统棋子几何。它必须回答：“按照当前公开攻击规则，进攻方是否能够使该将帅死亡或进入既定自动终结？”铁壁允许攻击线和将军跨墙属于明确的检查特例，即使普通棋子不能实际落入九宫吃子，也仍纳入攻击判断。

因此攻击来源包括：

- 普通明棋攻击；
- 铁马把将帅作为最终马步目标；
- 铁马把将帅作为马腿路径棋；
- 战车隔一枚非隐身路径棋攻击将帅；
- 战车把将帅作为路径棋碾碎；
- 出征将帅的车式攻击。

即使执行攻击会同时杀死进攻方将帅并导致平局，仍属于有效将军。平局是有效终局，受将方必须应对。

### 15.2 直接死亡矩阵

整次行动完成后统一检查：

| 红帅 | 黑将 | 结果 |
|---|---|---|
| 存活 | 存活 | 继续检查背刺、裁决或无处可逃 |
| 死亡 | 存活 | 黑方胜；若红方自己路径碾死红帅则为“乱杀” |
| 存活 | 死亡 | 红方胜；若黑将为敌方路径棋则为“碾碎他们！” |
| 死亡 | 死亡 | “两败俱伤”，平局 |

若将帅是最终将死目标而不是路径棋，沿用“裁决”。

### 15.3 已确认终局显示

| 规则名 | 原因代码 | 结果或播报 |
|---|---|---|
| 背刺 | `ambush` | `暗中潜行，破影而袭！您获得胜利！` |
| 裁决 | `checkmate` | `圣光的正义终结了敌人！您获得胜利！` |
| 无处可逃 | `stalemate` | 无合法走法的一方失败 |
| 臣服 | `resign` | `对方已臣服于您，获得胜利！` |
| 虚空放逐 | `disconnect` | `对方意外遭遇阿克蒙德，并不慎触怒了他。已被流放至扭曲虚空。` |
| 伏击 | `trap_ambush` | `猎物已踏入陷阱！伏击得手，红方／黑方获得胜利！` |
| 碾碎他们！ | `crush_them` | `碾碎他们！您获得胜利！` |
| 乱杀 | `rampage` | 仅碾死己方将帅，发动方失败 |
| 两败俱伤 | `mutual_destruction` | 双方将帅在同一次行动中死亡，平局 |

“乱杀”和“两败俱伤”尚未指定完整句式时，客户端只显示规则名和通用的失败/平局结果，不得自行增加世界观文案。

## 16. 动画与公开事件

现有 `LastMove` 只能记录一个目标，无法表达路径多杀、弹回和陷阱。建议改为服务器生成的有序事件：

```ts
type ActionEvent =
  | { kind: "move_started"; pieceId: string; from: Position; to: Position }
  | { kind: "effect_removed"; pieceId: string; effect: string }
  | { kind: "path_crushed"; pieceId: string; at: Position; identity?: SecretIdentity }
  | { kind: "target_captured"; pieceId: string; at: Position; identity?: SecretIdentity }
  | { kind: "barrier_bounce"; attackerId: string; defenderId: string }
  | { kind: "piece_revealed"; pieceId: string; identity: SecretIdentity }
  | { kind: "trap_triggered"; trapOwner: Side; at: Position; victimId: string }
  | { kind: "terminal"; outcome: GameOutcome };

interface ResolvedAction {
  actionId: string;
  revision: number;
  actingSide: Side;
  events: ActionEvent[];
}
```

客户端严格按事件顺序播放，不自行重新计算击杀、揭示或终局。终结动画播放完毕后才显示最终文案。

## 17. 联网数据边界

### 17.1 所有人可见

- 已公开的英雄选择；
- 当前畸变；
- 英雄/畸变技能剩余次数；
- 骑兵、普通防御、铁甲防御和隐身标记；
- 隐身剩余己方回合数；
- 陷阱触发事件，但不公开未触发陷阱；
- 有序行动事件和终局结果。

### 17.2 仅本人可见

- 自己尚未公开的英雄选择；
- 自己布置的陷阱坐标、叠层和剩余有效期。

### 17.3 仅服务器可见

- 全部未揭暗子真实身份；
- 双方尚未同时锁定时的英雄选择；
- 双方全部陷阱权威状态；
- 随机源和防重放记录。

原 `publicRemoteRoom(room)` 应升级为 `playerRoomView(room, playerId)`；旁观视图必须隐藏双方陷阱。

## 18. 必须新增的测试组

### 18.1 流程与保密

- `MODE-*`：四种开关组合都能进入正确阶段。
- `HERO-SELECT-*`：同选合法，双方锁定前不泄露。
- `MUTATION-*`：只抽一项、结果公开、测试随机可复现。
- `TRAP-SECRET-*`：敌方和旁观者永远看不到未触发坐标；本人刷新可恢复。

### 18.2 英雄

- `HUNTER-*`：半场、脚下、叠层、一次一层、十回合、控制权前后双判定、将帅伏击。
- `ROGUE-*`：发动目标、首次强击、两回合隐身、提前现身、强击清除效果、两次来源隔离。
- `WARRIOR-*`：前两枚不同棋、离宫触发、名额不回补、两步衰减、弹回、将帅铁甲与额外应将。

### 18.3 畸变

- `IRON-STEED-*`：空马腿、敌友暗明隐身马腿、空目标移动碾碎、目标防御、路径将帅。
- `IRON-WALL-*`：外部禁止进入、内部移动、渗透者离开后不能返回、强击不能突破、跨墙将军。
- `SHADOW-DANCE-*`：双方各一次、潜行者两次、不能并行潜行。
- `WAR-CHARIOT-*`：零/一/二枚普通路径棋、任意隐身路径、路径防御无效、目标防御有效、三种将帅结果。
- `EXPEDITION-*`：离宫、跨河、远距离移动、只检查落点、远程将军、陷阱死亡。
- `CAVALRY-*`：六个固定升级 ID、暗兵前向四马步、蹩马腿、揭示转移、非兵完整马步。

### 18.4 交叉与终局

- `CROSS-ROGUE-WARRIOR-*`：普通刺杀弹回、首次行动原地潜行、强击破防。
- `CROSS-CRUSH-ROGUE-*`：路径碾碎不消耗强击，最终目标可使用强击。
- `CROSS-CRUSH-WARRIOR-*`：路径防御无效、目标防御仍有效。
- `CROSS-HUNTER-*`：陷阱晚于落位揭示、早于背刺和裁决。
- `END-RAMPAGE-*`：仅己方将帅死亡判负。
- `END-MUTUAL-*`：双方将帅死亡判和。
- `END-CRUSH-*`：敌方将帅作为路径棋时使用专属播报。
- `CHECK-DRAW-THREAT-*`：会导致两败俱伤的攻击仍构成将军，不能误判为裁决。

## 19. 推荐实现顺序

为降低一次改动范围，按以下顺序实施：

1. 扩展 `GameOutcome`、有序事件和按玩家过滤的房间视图；
2. 加入模式开关、英雄选择和畸变抽取，不改变走棋；
3. 实现通用棋子效果框架与刺杀；
4. 实现战士防御和猎人陷阱；
5. 实现骑兵与出征两项移动扩展；
6. 实现统一路径碾碎，再接入铁马和战车；
7. 重写攻击/将军查询以覆盖路径终局；
8. 接入动画事件和全部终局文案；
9. 完成测试矩阵后才修改联机界面。

每一步都必须保证基础模式回归测试通过，不要把六项畸变一次性塞入现有 `movementGeometryLegal`。

## 20. 验收标准

进入功能编码前，本规格满足：

- 三名英雄与六项畸变均无待定玩法分支；
- 关闭新模式时与 v0.4.0 行为完全一致；
- 任意客户端都无法从落点提示、英雄锁定或房间推送获得暗子及陷阱秘密；
- 所有复合行动由服务器生成一个有序事件序列；
- 直接杀将、背刺、裁决和陷阱不会产生两个互相冲突的终局；
- 平局能够在类型层表达，不伪造胜方；
- 所有公开效果都由棋子 ID 跟踪，控制权改变不会丢失或复制效果。
