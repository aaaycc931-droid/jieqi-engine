# 实现状态与边界

更新时间：2026-09-18

## 已实现并由旧基线测试覆盖

- 基础揭棋规则、权威走子、已有终局结算和隐私净化。
- 三名英雄和六项畸变的旧规则版本。
- 潜行者与“暗影之舞”已升级为首次移动/立即强击二选一、随后一回合隐身；两个来源独立消耗。
- 已建立四稀有度目录并完成现有六项映射：堡垒为普通、骑兵为稀有，亲征/铁马/暗影之舞为史诗，战车为传说。
- 房间公开状态会同步畸变稀有度；揭示动画显示名称与文字稀有度，四档只变颜色、不改变动画强度；顶部入口常驻名称，点击后显示稀有度与完整规则。
- 已建立“外层选中传说后再构造内部候选池”的宿命接口；当前没有已确认的宿命 ID、英雄羁绊或具体效果，因此正式宿命目录保持空数组。
- 已定义普通首回合 60 秒、宿命首回合额外 15 秒和合计 75 秒常量；未在没有宿命候选的对局中误用。
- 本机双人流程、Bluetooth Classic 房主权威房间基础。
- 英雄选择 → 猜拳 → 畸变 → 英雄入场 → 英雄准备 → 正式行棋的阶段状态机。
- 猎人私有陷阱草稿、叠层、撤回、确认和超时随机补齐。
- V4 对战 UI 的透明组件拼合、32 枚棋子独立放置和批准素材接入。
- 棋子墨圈与字形整体再放大 7%，锚点和触控区不变；墨圈旋转约 4.6 秒一周。
- `npm run check`：144/144 测试通过，Web 构建成功；第二阶段新增稀有度映射、传说内部宿命候选、75 秒常量、房间公开同步和揭示/详情入口测试。

## 最新已确认但尚未完整实现

- 四稀有度的精确外层抽取权重；确认权重前，运行时仍保留兼容旧局的六项平抽，不把它称为最终抽取规则。
- 具体宿命畸变的 ID、名称、英雄羁绊、效果与专属动画；内容确认后才能让空候选目录产生实际宿命。
- 宿命实际触发后的双方各自首个正式回合 75 秒接线；底层常量已经建立，但当前没有可触发的具体宿命。
- 每名玩家单局累计断线 60 秒、跨阶段暂停恢复、同时超时平局及双方不同终局页面。
- 按阶段变化的三点菜单、`认输`命名和新的退出判负边界。
- 蓝牙 30 秒再战邀请、本机双人直接重开。
- 完整蓝牙状态页、玩法说明、中央详情层、聊天/快捷消息/历史、已吃棋子面板、状态展开和设置页。
- 新的系统返回、键盘、安全区域、字体缩放和减少动画规则。
- 非终局事件动画不锁棋盘、不停计时、按权威顺序播放。

## 已知旧实现冲突

- 当前畸变仍从六项中等概率抽取；已有稀有度元数据和宿命内部候选接口，但外层权重未定、正式宿命目录为空。
- Android 当前断线只暂停，尚未实现累计断线自动胜负。
- 当前菜单、再战和吃子栏仍是旧结构。
- 其余自动测试仍主要覆盖旧畸变、断线和 UI 基线；后续各阶段应先增加新规则测试，再替换实现。

## 未验证

- 累计断线、阶段菜单、再战以及其余最新 UI 的自动测试覆盖。
- 两台真实 Android 设备上的累计断线、自动重连、秘密状态和完整蓝牙再战。
- 不同字体缩放、导航栏模式、刘海/挖孔和不同竖屏比例。
- Release 签名、商店发布和互联网联机。

## Git 边界

- 当前实现分支：`codex/phase2-mutation-rarity-20260918`，基于第一阶段 `codex/phase1-rogue-20260918`。
- 基线分支：`codex/ui-v4-apk-test-20260915` @ `de4eb73bcaea678e1f183ec2f0181369fe1cebce`。
- `main` 最近核验为 `c25749d4f21d394c3d94486cfba1d786864083b6`。
- 本检查点不合并 `main`、不部署，也不提交交接压缩包和 APK。
