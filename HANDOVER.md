# 乐子象棋交接说明（2026-09-18）

> 本文件区分旧实现与最新规划。最新决定以根目录 `CONFIRMED_UI_PLAN.md`、`BALANCE_AND_RULE_UPDATES.md`、`IMPLEMENTATION_STATUS.md`、`PROJECT_STATE.json` 和 `PENDING_AND_RESUME.md` 为准；旧代码中的潜行者、畸变抽取、断线、菜单和再战逻辑不得作为最新产品规则。

## 当前状态

- 本地试玩与 Android 蓝牙双机共用同一套规则和页面；应用显示名为“乐子象棋”，作者显示为“流风舞影”。
- 当前产品流程固定启用英雄与单局随机畸变：英雄选择 → 石头剪刀布 → 棋局与畸变 → 英雄入场 → 英雄准备 → 正式行棋。
- `RemoteRoom` 与 `BluetoothHostRoom` 已实现上述阶段、两段 60 秒超时、私有英雄选择与猎人陷阱草稿、房主权威随机补齐。
- 依赖 Node.js 24+，无第三方 npm 依赖。
- 当前验证基线：`npm run check` 通过 135 项测试并生成网页；这些测试仍包含旧潜行者和旧独立吃子栏用例。
- 本分支只建立规划定稿检查点；尚未实现 2026-09-18 新决定，不合并 `main`，也不部署互联网联机。

## 当前开局流程

1. 两名玩家到齐后进入 60 秒英雄选择。无默认英雄，必须先点英雄再确认；双方都确认则立刻进入猜拳。
2. 到时只为未确认玩家随机英雄，保留已经确认的选择；本人短暂看到随机结果。猜拳前不显示对方英雄，同英雄合法。
3. 石头剪刀布胜者执红并先走，平局重猜。
4. 创建棋局并公开本局随机畸变；页面依次播放畸变公布和约 2 秒英雄水墨入场。英雄形象暂用占位，不做阵营翻转、镜像或改色。
5. 头像落位后才开始最多 60 秒的通用英雄准备。无战斗开始技能的英雄自动完成；所有需要操作的玩家完成后立即开始正式行棋。
6. 猎人点击己方半场格点逐层放置，两层可同格、可在棋子脚下；“撤回上一步”删除最后一层草稿，“完成准备”锁定。到时保留已放草稿并随机补齐剩余层。

## 英雄选择页面

- 页面高度按上方英雄详情 1/4、下方英雄网格 3/4 分配；当前网格不纵向滚动。
- 竖屏每行 6 个英雄，按从左到右、从上到下排列，末行左对齐，不补空格。
- 网格项不使用卡片外框，只显示头像与单行英雄名；长名称缩小且不换行。
- 详情区左侧为头像；右侧上方显示英雄名，技能图标横排，图标下方显示技能名；点击切换短说明，长按查看完整说明。
- 底部留白动作条左侧为倒计时、中间为对手状态、右下角为“确定”。最后 10 秒使用中性黑墨脉冲，不使用阵营颜色。

## 旧实现与最新决定的关键差异

- 旧实现中潜行者第一次刺杀只能移动到空位且隐身两回合；最新规则允许首次立即强击，并把隐身缩短为下一个己方回合。
- 对外显示名使用“堡垒”和“亲征”；协议 ID `iron_wall`、`expedition` 保持不变。
- 陷阱普通击杀只播报棋子死亡；只有将帅被陷阱击杀时才宣布伏击胜利。
- 旧实现从六项畸变中等概率抽取；最新规则已引入四种稀有度及隐藏宿命传说候选，但精确外层概率暂缓。
- 旧 Android 逻辑断线只暂停；最新规则要求每名玩家单局累计断线 60 秒判负，并从原阶段/动画节点恢复。
- 旧 UI 菜单、吃子栏和再战确认已被最新规划废止，具体见 `CONFIRMED_UI_PLAN.md`。

## 关键入口

| 文件 | 用途 |
|---|---|
| `README.md` | 运行、构建和项目概览 |
| `HERO-MUTATION-SPEC.md` | 英雄、畸变与开局阶段规格 |
| `CONFIRMED_UI_PLAN.md` | 已收口的功能性 UI 与交互规则 |
| `BALANCE_AND_RULE_UPDATES.md` | 新潜行者、评分、稀有度、宿命与组合禁用规则 |
| `IMPLEMENTATION_STATUS.md` | 已实现、待实现与旧规则冲突清单 |
| `PENDING_AND_RESUME.md` | 有意暂缓内容与推荐实施顺序 |
| `ONLINE-ARCHITECTURE.md` | 远程房间的数据边界与阶段契约 |
| `src/remote-room.ts` | 权威房间状态机、超时与私有数据视图 |
| `src/bluetooth-host-room.ts` | 蓝牙房主协调器与动作路由 |
| `web/index.html` / `web/style.css` / `web/app.ts` | 当前完整页面与本机/蓝牙交互 |
| `tests/remote-room.test.ts` | 英雄选择、入场、准备和超时测试 |
| `tests/web-layout.test.ts` | 英雄页面与准备阶段静态结构测试 |
| `android/` | Android WebView 与 Bluetooth Classic 外壳 |

## 下一步

1. 先做代码—规格差异审计，不直接修改暂缓的状态栏布局。
2. 分阶段实现新潜行者、稀有度/宿命、首回合 75 秒和累计断线终局，并先补自动测试。
3. 再补阶段菜单、详情层、蓝牙再战、聊天、吃子记录、设置和玩法说明。
4. 最后进行两台真实 Android 设备的完整蓝牙回归。
5. 正式英雄美术、宿命专属动画、精确稀有度概率、状态栏位置和互联网联机仍等待用户主动恢复。
