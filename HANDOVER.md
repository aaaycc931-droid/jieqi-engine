# 乐子象棋跨账号交接说明（2026-09-24）

> 本轮已正式收束。新账号不得从旧交接重新规划，也不得把“累计断线 60 秒”恢复为待实现项。

## 1. 当前实现检查点

- 仓库：`aaaycc931-droid/jieqi-engine`
- 当前工作分支：`codex/phase3-cumulative-disconnect-20260923`
- 本轮实现基线：`30ba8486cc9bdeaf9ceb7f5e0a379e1617fd3846`
- 本轮最终实现状态提交前一版：`c331a0a6128ab8c2f977a4163a31acb8001f8f67`
- 本轮状态文档提交：`045042cc0bc7e64dbd493e4052cdaebd9f0a0d89`
- `main` 最后核验：`c25749d4f21d394c3d94486cfba1d786864083b6`
- 未合并 `main`，未部署互联网联机。

## 2. 本轮已经完成

### 累计断线 60 秒
- 每名玩家在单局中独立累计断线时间，多次断线不清零。
- 从首次连接成功并进入英雄选择开始纳入断线规则。
- 任一玩家断线后，双方操作、英雄选择/准备倒计时、普通回合视觉倒计时及开场动画暂停。
- 重连后从原剩余阶段时间/动画节点继续。
- 单方最先累计达到 60 秒：该方判负，终局 reason 为 `disconnect`。
- 双方在同一实际阈值时刻达到 60 秒：平局，`drawReason = disconnect_timeout`。
- 如果双方断线起点不同，即使系统较晚才检查，也按真实达到 60 秒的时刻先后裁定，不把错峰超时误判为平局。
- 胜方文案保持：“对方被流放至扭曲虚空。您获得胜利！”
- 断线失败者恢复可显示后，弹窗告知超时失败并返回主菜单。
- 断线平局恢复后显示平局提示并返回主菜单。

### Android / Web 重连
- Android Bluetooth Classic 传输层加入自动重连基础。
- 房主和来宾会复用原角色；来宾复用原设备地址。
- 本机蓝牙关闭时桥接层可报告 `adapterEnabled=false`，Web 显示“请开启蓝牙”并继续累计时间。
- Web 加入断线遮罩、累计/剩余时间、操作锁和 2 秒“连接已恢复”提示。
- 开场畸变/英雄入场动画与普通回合视觉倒计时在断线时暂停。

### CI / APK
- 自动测试现为 **150/150 通过，0 失败**。
- Web 构建成功。
- Android CI 已改为：先构建并同步当前 WebView 资源，再编译 APK，避免 APK 携带旧网页。
- GitHub Actions 验证 run：`35950543824`，rules-and-web 与 android-apk 均成功。
- 对应 APK artifact：`jieqi-bluetooth-debug-apk`，artifact id `10788104035`。
- 该 APK 已生成用于两机测试；APK/ZIP 不提交到仓库。

## 3. 当前唯一优先下一步

**两台真实 Android 设备进行累计断线/自动重连真机验证。**

建议按以下场景测试：
1. 对局中单次短暂断线后自动恢复。
2. 同一玩家多次断线，确认累计时间不清零。
3. 房主本机关闭蓝牙再开启。
4. 来宾本机关闭蓝牙再开启。
5. 对手链路异常后恢复。
6. 单方累计达到 60 秒，核对胜负和双方不同终局表现。
7. 双方尽可能同时断线达到 60 秒，核对平局。
8. 双方错峰断线并都超过 60 秒，核对先到阈值者判负。
9. 英雄选择、猜拳、英雄入场、英雄准备、正式行棋各阶段分别断线，确认倒计时/动画恢复节点正确。
10. 核对来宾始终看不到房主暗子真实身份、对方未公开英雄选择及猎人陷阱私有坐标。

发现真机问题后：**先复现和定位本阶段断线/重连代码，再修；不要跳去实现下一功能。**

## 4. 真机验证通过后的实现顺序

1. 阶段化三点菜单、`认输`命名与退出判负边界。
2. 蓝牙 30 秒再战邀请、本机双人直接重开。
3. 聊天、快捷消息、事件历史。
4. 已消灭/吃子记录、状态展开、设置、玩法说明等已确认功能。
5. 继续遵守“测试先行 → 实现 → 完整回归”。

## 5. 仍有意暂缓

以下内容不得为了“完成度”自行决定：
- 状态栏具体高度、内部按钮位置和尺寸。
- 正式英雄头像和技能图标。
- 宿命畸变专属揭示动画视觉方案。
- 普通四稀有度精确外层概率。
- 互联网联机。

## 6. 本轮规则复核已经闭合

此前交接包第 7～10 项已经全部确认并写回规则事实源，包括：
- 【无形】默认保留正常攻击几何与有效将军；潜行者来源无形期间“不产生有效将军”为来源专属限制。
- 多步骤原子结算采用“候选终局 → 原子闭合 → 正式终结”。
- 时光勇士任何公开【揭示】（含死亡揭示）均建立并永久保留“已现身”历史状态。
- 感染/窒息/时间崩坏等特殊杀将采用稳定 reason ID 映射，并保留通用 `general_destroyed` 兜底；同一原子结算双方将帅最终均消灭时提升为 `mutual_destruction`。

不要重新打开这些项目，除非发现实际冲突或用户主动要求修改。

## 7. 关键文件读取顺序

新账号开始时：
1. `START_HERE.md`
2. `PROJECT_STATE.json`
3. `IMPLEMENTATION_STATUS.md`
4. `PENDING_AND_RESUME.md`
5. `CONFIRMED_UI_PLAN.md`
6. `BALANCE_AND_RULE_UPDATES.md`
7. `RULES-CHANGELOG.md`
8. 需要代码细节时再读 `src/remote-room.ts`、`src/bluetooth-host-room.ts`、`web/app.ts` 和 Android 蓝牙桥接层。

## 8. 禁止事项

- 未经明确授权不得合并 `main`。
- 不部署互联网联机。
- 不把交接 ZIP 或 APK 提交仓库。
- 不擅自重绘已经批准的 UI 资产。
- 不用旧规格覆盖后来的确认。
- 不把 CI 通过等同于真机 Bluetooth RFCOMM 已验证。
