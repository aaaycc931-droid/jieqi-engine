# 乐子象棋 UI 与交互最终确认稿

状态：2026-09-18 功能性 UI 规划已收口。除本文“有意暂缓”项目外，后续不得重新提议、擅自补全或恢复旧方案。

## 一、全局视觉与适配

- 竖屏水墨、宣纸、颜体视觉体系；不以 9:16 为唯一基准，正方形棋盘保持比例，通过调整纵向间距适配不同设备。
- Android 状态栏、刘海、挖孔、导航栏和安全区域必须避让；正式棋盘页不可整体纵向滚动，长内容页允许正文滚动。
- 边框有淡浓变化，四角略浓且锋利，不改成规整矩形直线。
- 棋盘、棋子、边框、背景和动态文字分层；运行时使用透明 PNG 组件拼合。禁止从最终整图二次抠图、整排贴图、白色遮线和模糊重绘。
- 棋子中心保持透明并显示原棋盘纹理；棋子锚点、棋盘交叉点和触控区不变。
- 当前批准棋子尺寸为：既有墨圈外圈放大 15% 后，墨圈与字形整体再放大 7%；小型展示棋子不跟随这次 7% 放大。墨圈旋转一周约 4.6 秒。
- 所有主要触控目标不小于 48dp；颜色不能作为唯一状态信息。
- 系统字体缩放适用于一般文字；棋盘坐标、棋子字形和计时器按界面比例固定，避免破坏棋盘结构。

## 二、启动、主菜单与设置

- 不制作自定义启动页；应用启动后直接进入主菜单，Android 系统启动画面不算游戏启动页。
- 主菜单按钮：`本机双人`、`蓝牙对局`、`联机对战`、`玩法说明`。
- `联机对战`当前禁用，点击显示淡出的“联机对战尚未开放”。
- 主菜单右上角使用小型水墨设置图标，不占主按钮区域。
- 当前设置至少提供：音效、触感反馈、减少动画；不写死为永久仅有这三项。
- 音效默认开启并跟随系统媒体音量；当前不加入背景音乐和独立音量滑杆。
- 触感反馈默认开启，只用于落子、技能确认、倒计时警告、终局等关键节点。
- 减少动画默认关闭；开启后缩短动画，但保留畸变揭示、英雄入场、终局结果等必要信息。
- 正式对局及全部准备阶段保持屏幕常亮；主菜单和蓝牙等待页恢复系统休眠规则。
- 设置即时保存在本机，不设确认按钮，也不与对手同步。设置页顶部标题和返回键固定。

## 三、本机双人与蓝牙入口

- 本机双人继续使用水墨交接弹窗“请交给下一位玩家”，不新增全屏交接页面。
- 切换玩家前清除上一位玩家的英雄选择、猜拳选择和猎人未公开陷阱显示。
- 蓝牙对局使用独立页面和固定顶部标题/返回键；同一页面承载蓝牙关闭、权限请求、初始选择、创建、加入、搜索、连接、已连接、失败、重试与赛后再战状态。
- 蓝牙关闭时显示`开启蓝牙`；权限拒绝后解释用途并允许重试，永久拒绝时引导前往系统设置。
- 初始状态显示`创建对局`和`加入对局`。创建方显示设备名、等待状态和取消；加入方显示已配对设备及可用状态。
- 没有已配对设备时显示说明、打开系统设置和重新检测。连接中显示设备名与取消；失败显示原因、重试和返回。
- 首次连接成功仅短暂显示“连接成功”，随后自动进入英雄选择，不增加准备按钮。
- 主动断开需要确认；断开后返回蓝牙初始页。页面不显示上一局英雄、畸变或棋盘信息。

## 四、英雄选择与保密

- 页面上方约 1/4 显示当前英雄头像、名称、技能图标与说明；下方约 3/4 为英雄网格，每行六个、左对齐，当前英雄数量下不滚动。
- 右下角为确定按钮。确认前可以更换；确认后不能撤销。
- 倒计时 60 秒；双方都确认立即结束，超时只为未确认者随机英雄。
- 蓝牙对局只显示对方“未确认/已确认”，猜拳前不公开对方英雄；双方英雄在进入棋盘的入场动画中公开。
- 本机双人每名玩家确认后进入保密交接流程。

## 五、石头剪刀布

- 先选择、再确认；不是点中即锁定。
- 三个水墨图形；当前选择出现墨晕，其余变淡。确认后隐藏具体选择，只显示“已锁定”。
- 蓝牙等待对方；本机双人使用保密交接。
- 双方同时揭示，平局重新选择。
- 倒计时 30 秒；超时只为未确认者随机选择。

## 六、畸变揭示、英雄入场与准备

1. 显示猜拳结果与执红/先手归属。
2. 进入正式对战 UI，棋盘暂时锁定。
3. 中央揭示本局畸变名称、稀有度文字和一句摘要。
4. 普通、稀有、史诗、传说使用相同动画强度，只通过克制颜色与稀有度文字区分。
5. 宿命畸变未来使用单独设计的专属揭示动画。
6. 揭示结束后信息收缩到中央计时器下方的“本局畸变”；常驻状态不显示稀有度，点击详情后显示。
7. 双方英雄按各自固定方向水墨入场，不因红蓝阵营镜像；头像落位后进入英雄准备。

- 英雄准备复用正式棋盘，不制作独立页面；普通行棋锁定，倒计时 60 秒。
- 猎人在己方半场布置陷阱，允许放在棋子脚下和同格叠层；提供撤回与确认。
- 超时保留已布置陷阱并随机补齐剩余层数。
- 蓝牙双猎人同时秘密准备；本机双人依次准备并在交接前清除上一人的秘密标记。
- 没有开局操作的英雄显示“无需准备”，不额外等待。

## 七、正式对战页

- 顶部中央为无边框纯数字倒计时，普通回合从 60 秒开始；其下显示“本局畸变”。
- 状态栏不额外显示“行动中”，也不使用状态栏边缘变化强调当前行动方；当前回合由中央计时器与棋盘可操作状态表达。
- 独立“已吃棋子”栏及中央展开箭头正式取消；双方各在自己的玩家状态栏中拥有已吃棋子按钮。
- 状态剩余回合数优先显示在对应棋子上；过多状态缩略，点击关键词查看。
- 已播放的中局或终局动画不再叠加重复说明文案。

## 八、玩家状态栏与技能按钮

- 每条状态栏常驻信息：玩家名称、阵营文字、英雄头像、英雄名称、技能按钮、已吃棋子数量；中央计时器不重复放入状态栏。
- 点击任一方英雄头像，打开该英雄完整技能规则和本局实时状态，但不泄露陷阱位置、暗子身份等秘密信息。
- 己方主动技能单击进入目标/落点选择，不立即消耗；选定合法目标并完成操作后才消耗。再次点击技能按钮或按返回键可取消。
- 长按己方技能按钮打开说明。对方技能不能发动，因此点击或长按都打开说明。
- 被动技能按钮标注“被动”，点击或长按都打开说明。
- 状态同时使用明暗、文字与必要数字：`可用`、`选择中`、`已用`、具体不可用原因、公开剩余回合。
- 潜行者遇到“暗影之舞”时，英雄技能与畸变技能显示为两个独立按钮，标注`英雄`和`畸变`，分别记录和消耗。
- 状态栏最多常驻三个重要状态；其余折叠为`+N`。点击后展开该玩家全部公开状态；棋盘锁定、计时继续。
- 技能可用性、次数和公开持续时间双方可见；陷阱坐标与同格层数、未公开暗子身份不得泄露。
- 断线层和终局结果层出现后，状态栏停止交互。
- 状态栏高度、内部按钮位置和具体尺寸仍有意暂缓。

## 九、已吃棋子按钮与面板

- 每个按钮只统计该玩家造成离场的对方棋子，显示“已吃”和数量。
- 点击后打开半透明水墨浮层；双方都能查看两边记录。点击外部、关闭按钮或系统返回键收起。
- 面板打开时棋盘不可操作，回合倒计时继续；断线层和结果层出现后不能打开。
- 棋子按实际离场顺序从左到右、从上到下排列，不按兵种合并；最多三行、每行五枚。
- 暗子被吃后按已揭示的真实身份显示，并同时提供阵营文字/标记，不能只靠颜色。
- 陷阱、强击、铁马和战车等造成的敌方棋子离场，都计入发起方记录。
- 玩家吃掉己方暗子时不计入任何一方的“已吃棋子”，只写入对局历史。
- 数量更新时播放很短的淡墨扩散，不弹窗、不锁棋盘。
- 空记录显示“尚未吃子”。终局结果层中不可再打开，但最终记录保存在本局结果数据中；当前不提供历史对局入口。

## 十、规则、畸变与技能详情层

- `对局规则`、`本局畸变`、英雄和技能说明统一使用棋盘上的中央半透明水墨宣纸覆盖层，不跳转独立页面。
- 打开时棋盘不可操作、回合倒计时继续；标题与关闭按钮固定，正文区域滚动。
- `本局畸变`只显示本局畸变的名称、稀有度、完整规则和关键交互。
- `对局规则`只显示通用规则，按主题折叠并默认展开第一组。
- 不存在独立“双方英雄技能说明”菜单或中间页；入口只有双方英雄头像和技能按钮。
- 技能说明同时显示固定规则及本局实时状态，只展示实际适用字段。

## 十一、三点菜单与系统返回

- 三点菜单从英雄选择开始提供；按阶段显示不同条目：
  - 英雄选择、猜拳：`对局规则`、`退出对局`。
  - 畸变揭示、英雄入场、英雄准备：`对局规则`、`本局畸变`、`退出对局`。
  - 正式对局：`对局规则`、`本局畸变`、`认输`。
- 菜单不包含`继续对局`、`快捷消息`或独立`英雄技能说明`。
- 菜单由按钮或系统返回键打开，显示为按钮上方的小型水墨面板；点击外部或再次返回关闭。
- 菜单打开时棋盘锁定、倒计时继续。
- `认输`确认文案：“确定要认输吗？认输后本局将立即结束。”按钮为`继续对局 / 确认认输`。
- 胜方结果文案仍使用“对方已臣服于您，获得胜利！”。
- 首次连接前离开不产生结果；进入英雄选择后主动退出按本局失败处理。
- 断线重连层和终局结果层不提供三点菜单。
- 主菜单系统返回直接退出应用；普通页面返回上一页；覆盖层返回关闭最上层；结果层返回/关闭回到主菜单。

## 十二、聊天、快捷消息与历史

- 正式蓝牙对局提供聊天；英雄选择、猜拳、揭示、准备和赛后等待页不提供聊天。
- 本机双人只显示对局历史，不显示输入框或快捷消息。
- 默认底部消息栏收起，只显示最新一条系统或聊天消息；点击后从底部展开，最多覆盖棋盘下半部分。
- 展开时棋盘不可操作、回合计时继续；点击箭头或下滑收起。
- 己方消息靠右、对方靠左；系统记录居中，字号更小、颜色更淡、间距更紧。
- 快捷消息两列共八条：`你好！`、`好棋！`、`走得漂亮`、`真险`、`我再想想`、`稍等一下`、`谢谢`、`承让`。点击即发，不二次确认。
- 自定义消息最多 50 个字符，允许文字、数字、常用符号与表情；不支持图片、语音和换行。纯空白不可发送，首尾空格自动清除。
- 快捷消息与自定义消息共用 2 秒发送间隔；冷却时控件变灰，不排队、不弹错。
- 键盘出现时不压缩或移动棋盘；面板位于键盘上方并缩短历史区，顶部计时仍可见。
- 发送后清空输入框，键盘和面板保持打开。
- 收起状态出现新内容时显示无数字小墨点；展开后滚动到最新消息才清除。查看旧记录时新消息不强制跳底，显示“有新消息”。
- 断线时禁止发送但保留未发送草稿；恢复后继续编辑。因断线终局时清除草稿。
- 正式事件按顺序进入历史；普通非法点击等即时反馈不写入历史。
- 新一局进入英雄选择后，清空上一局聊天与历史；不提供跨局聊天记录或导出。
- 直接蓝牙好友对局不增加举报、屏蔽或敏感词审查系统。

## 十三、非终局事件

- 将军、普通陷阱触发、护甲破裂、战车碾碎等不会立即结束对局的事件，使用短暂自动消失的水墨提示，不设确认按钮。
- 动画不锁棋盘、不拦截触控，不暂停或重置回合倒计时。
- 同一行动连续触发多个事件时，按权威结算顺序依次播放，不互相重叠。
- 终局仍先播放终结效果，再锁定棋盘并显示结果层。

## 十四、断线累计、重连与断线终局

- 每名玩家在单局中分别累计断线时间；单次或多次累计达到 60 秒，该玩家直接判负。
- 计时从双方首次连接成功并进入英雄选择开始，覆盖英雄选择、猜拳、畸变揭示、英雄入场、英雄准备和正式行棋；首次连接建立前的配对失败不计入。
- 断线后暂停当前阶段倒计时和动画，锁定所有操作，并自动尝试重连。
- 重连层保留变暗棋盘；中央水墨面板显示断线玩家头像/名称、重连状态、已累计时间和剩余时间。
- 本机蓝牙关闭时显示`请开启蓝牙`按钮，断线累计计时仍继续。
- 恢复连接后显示 2 秒“连接已恢复”，随后从原剩余时间和原动画节点继续。
- 重连层没有认输或退出按钮；每次成功重连写入系统历史。
- 断线者达到 60 秒后，不进入游戏内失败结果层；设备恢复可显示时，弹窗告知上一局因断线超时失败，关闭后进入主菜单。
- 对手看到最终棋盘和正常结果层，胜利文案为“对方被流放至扭曲虚空”。
- 双方同时达到 60 秒判为平局；恢复后弹窗告知结果并返回主菜单，不进入游戏内结果层。

## 十五、终局与再来一局

- 正常终局保留最终棋盘背景，锁定棋盘并播放终结效果后，中央展开半透明水墨结果层。
- 结果层显示胜利、失败或平局、终局类型、终局文案、胜方阵营/玩家/英雄，并提供`再来一局`和`返回主菜单`。
- 蓝牙模式点击`再来一局`后立即离开结果层和旧棋盘，进入蓝牙页的已连接状态并向对方发出邀请；旧结果不能返回。
- 对方可在 30 秒内接受或拒绝；接受后进入新英雄选择，英雄、阵营、猜拳和畸变全部重置。
- 拒绝提示“对方已拒绝再战”；超时提示“对方未响应再战请求”；断线提示“对方已断线”。关闭提示后都停留在蓝牙页，不返回旧结果层，可稍后再次发起。
- 本机双人共用一台设备，不发送邀请、不进行第二人确认、没有 30 秒等待。点击`再来一局`立即关闭结果层并进入新英雄选择，然后继续既有保密交接流程。

## 十六、玩法说明页

- 顶部固定四类：`基础玩法`、`英雄技能`、`畸变规则`、`联机规则`；分类标签、页面标题和返回键固定，正文滚动。
- 每次进入默认打开基础玩法顶部，不记忆上次位置。相关项目使用单项展开结构，同一时间只展开一项；不设确认按钮。
- 重要数字使用克制的墨色/朱砂强调，不闪烁；规则图可点击放大。
- 基础玩法包含：对局流程、暗子与揭棋、移动与吃子、河界与九宫、将军与应将、胜负及特殊终局；提供流程图与完整棋子走法图，自吃规则重点标注。
- 英雄技能按猎人、潜行者、战士的加入顺序显示；卡片常驻头像、名称、图标和一句定位，展开后显示类型、次数、时机、目标、操作、持续、结束和交互。内部评分不展示，羁绊和宿命不公开。
- 畸变规则首页只显示普通、稀有、史诗、传说四种普通稀有度；普通淡墨、稀有青墨、史诗紫墨、传说金褐，并始终配有文字。
- 点击稀有度进入同页单列文字名称列表；点击名称展开完整规则。只说明稀有度越高越少见，不公布概率和内部评分。宿命不在说明页出现。
- 联机规则分为建立连接、流程、信息同步与保密、断线重连、断线胜负，反映本文已确认规则。

## 十七、有意暂缓与范围边界

- 状态栏具体高度、内部按钮位置和尺寸：暂缓，不得自行确定。
- 正式英雄头像和技能图标：未最终制作，不得擅自定稿。
- 宿命畸变专属揭示动画：只确认需要独立设计，视觉方案暂缓。
- 普通四稀有度的精确抽取概率：等待畸变池扩充后再定。
- 互联网联机模式：入口保留但禁用，不部署、不实现。

除以上五项外，功能性 UI 规划已经完整结束。后续工作应进入文档同步、技术差异审计和分阶段实现，不再从头规划已确认页面。
