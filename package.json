{
  "name": "jieqi-engine",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test --test-reporter=spec tests/*.test.ts",
    "dev": "node scripts/build-web.ts && node scripts/serve-web.ts",
    "build:web": "node scripts/build-web.ts",
    "serve:web": "node scripts/build-web.ts && node scripts/serve-web.ts",
    "check": "node --test --test-reporter=spec tests/*.test.ts && node scripts/build-web.ts"
  },
  "engines": {
    "node": ">=24"
  }
}
